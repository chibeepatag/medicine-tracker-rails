<%= form_with(model: event, url: patient_event_path, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_date %>
    <%= form.date_select :event_date, id: :event_event_date %>
  </div>

  <div class="field">
    <%= form.label :severity %>
    <%= form.text_field :severity, id: :event_severity %>
  </div>

  <div class="field">
    <%= form.label :organ %>
    <%= form.text_field :organ, id: :event_organ %>
  </div>

  <div class="field">
    <%= form.label :reaction %>
    <%= form.text_field :reaction, id: :event_reaction %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
